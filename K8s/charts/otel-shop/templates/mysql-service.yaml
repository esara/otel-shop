apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mysql
  labels:
    service: {{ .Release.Name }}-mysql
spec:
  ports:
  - name: {{ .Release.Name }}-mysql
    port: 3306
  selector:
    service: {{ .Release.Name }}-mysql
